import { MyStructDirectiveDirective } from './my-struct-directive.directive';

describe('MyStructDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MyStructDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
